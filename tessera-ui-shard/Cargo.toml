[package]
name = "tessera-ui-shard"
version = "0.1.0"
description = "Provide easier state access and router for Tessera UI."
license.workspace = true
edition.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
