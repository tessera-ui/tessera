# {{project_name}}

Tessera plugin crate with an Android JVM module.

## Usage

Add the plugin to your Tessera app:

```toml
[dependencies]
{{project_name}} = { path = "{{project_name}}" }
```

Register it in your entrypoint (recommended):

```rust
use tessera_ui::EntryPoint;

#[tessera_ui::entry]
pub fn run() -> EntryPoint {
    EntryPoint::new(app).package({{project_name_snake}}::HelloPackage::default())
}
```

You can also register the plugin directly:

```rust
use tessera_ui::EntryPoint;

#[tessera_ui::entry]
pub fn run() -> EntryPoint {
    EntryPoint::new(app).plugin({{project_name_snake}}::HelloPlugin::default())
}
```

Use it from components or app code:

```rust
{{project_name_snake}}::with_plugin(|plugin| {
    println!("{}", plugin.message());
});
```

## Android module

The Android JVM code lives under `android/` and exposes
`{{android.package}}.HelloPlugin`. When you run `cargo tessera android init`,
`build`, or `dev`, the module is synced into `gen/android/plugins/{{project_name_snake}}`.

## Build

```bash
cargo build
```
