# {{project_name}}

A minimal Tessera UI starter that renders a full-screen surface with a
`Hello Tessera!` label using the current builder-style component syntax.

## Running

```bash
cargo tessera dev
```

## Building

```bash
cargo tessera build --release
```
